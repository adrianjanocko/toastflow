---
title: Events
description: Subscribe to Toastflow lifecycle events for analytics, debugging, and UI reactions, including duplicates, updates, and timer resets.
navigation:
  icon: i-tabler-radio
---

Toastflow emits lightweight events through `subscribeEvents`.

::note
Event types are defined in [`packages/core/src/types.ts`](https://github.com/adrianjanocko/toastflow/blob/main/packages/core/src/types.ts){target="\_blank"} — look for `ToastEvent` and `ToastEventKind`.
::

## Subscribe

::code-group

```ts [subscribe.ts]
const off = toast.subscribeEvents((event) => {
  console.log(event.kind, event.id);
});
```

```ts [unsubscribe.ts]
// call the returned function to stop listening
off();
```

::

## Event Kinds

::field-group

::field{name="duplicate" type="ToastEventKind"}
`show(...)` found an existing match while `preventDuplicates: true`.
::

::field{name="timer-reset" type="ToastEventKind"}
A visible toast timer was reset (update, or resume with `pauseStrategy: "reset"`).
::

::field{name="update" type="ToastEventKind"}
`update(id, options)` succeeded.
::

::

Each event is a `{ id, kind }` object — `id` is the affected toast's `ToastId`.

## Behavior Notes

::accordion
::accordion-item{label="duplicate + update overlap"}
Both events can fire in the same duplicate handling flow, depending on the action path.
::

::accordion-item{label="Queued toast updates"}
Updating a queued toast emits `update` but not `timer-reset` — no active timer exists yet.
::

::accordion-item{label="Separate from state stream"}
The event stream does not carry state snapshots. Use `subscribe(listener)` for state.
::
::

## Typical Uses

- Trigger CSS bump or progress reset animations
- Analytics and telemetry for notification flows
- Debug queue and timer behavior during development
