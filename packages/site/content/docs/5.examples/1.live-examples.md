---
title: Live Examples
description: Trigger real Toastflow toast notifications from the docs with types, positions, actions, queues, and theming examples you can run live.
navigation:
  icon: i-tabler-player-play
---

Use these examples to feel the runtime behavior before copying code into your app. They mirror the examples from the previous documentation.

::note
The examples run in the browser through the Vue REPL. They use the same Toastflow store, timers, queue logic, and action handling as a real app.
::

<DocsLiveExamples />

## Copy the Same Patterns

::code-tree{default-value="success.ts" expand-all}

```ts [success.ts]
toast.success({
  title: "Saved",
  description: "Your changes are live.",
});
```

```ts [loading.ts]
await toast.loading(fetch("/api/save"), {
  loading: { title: "Saving" },
  success: { title: "Saved" },
  error: { title: "Failed" },
});
```

```ts [action.ts]
toast.info({
  title: "File archived",
  buttons: {
    alignment: "bottom-right",
    buttons: [
      {
        label: "Undo",
        dismissAfterClick: true,
        onClick() {
          toast.success({ title: "Restored" });
        },
      },
    ],
  },
});
```

::

## Where to Use Them

::card-group
::card{title="Vue" icon="i-simple-icons-vuedotjs" to="/docs/vue/quick-start"}
Use the same `toast.*` helpers after `createToastflow()` is installed.
::

::card{title="Nuxt" icon="i-simple-icons-nuxtdotjs" to="/docs/nuxt/toast-and-use-toast"}
Use the auto-imported `toast` or grab the same API with `useToast()`.
::

::card{title="Headless" icon="i-tabler-automation" to="/docs/headless/core-store"}
Use `toastflow-core` when you want the behavior without Toastflow's Vue UI.
::
::
